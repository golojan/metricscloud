{
  "version": 2,
  "builds": [{ "src": "./server/main.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/(.*)", "dest": "/server/main.js" },
    { "src": "/api(.*)", "dest": "/server/main.js" }
  ]
}